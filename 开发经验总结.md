# 《无限数域》开发经验总结

> 项目：Infinite Numeral v1.0 + v2.0 "维度裂变"
> 技术栈：Vue3 + TypeScript + Pinia + Capacitor Android
> 仓库：https://github.com/netbaker/infinite-numeral.git

---

## 一、项目架构经验

### 1.1 状态管理：为什么用 `markRaw` + `stateVersion`

**背景**：游戏状态 `gameState` 是一个非常大的嵌套对象，包含 `Decimal` 类型（来自 `break_eternity.js`）。如果直接用 `reactive()` 包裹，Vue 会递归代理所有属性，导致严重性能问题。

**方案**：
```ts
// gameStore.ts
const gameState = markRaw(reactive<GameState>({ ... }));
const stateVersion = ref<number>(0);

function bumpVersion() {
  stateVersion.value++;
}
```

- `markRaw` 阻止 Vue 深度代理，提升性能
- `stateVersion` 是一个简单的 `ref`，每次状态变化手动 `bumpVersion()`
- 组件中通过 `void gameStore.stateVersion` 强制追踪变化

**教训**：所有访问 `gameState` 的 `computed` 都必须加入 `void gameStore.stateVersion`，否则 UI 不更新。

---

### 1.2 `Decimal` 类型（break_eternity.js）使用规范

| 操作 | ✅ 正确 | ❌ 错误 |
|------|---------|---------|
| 比较大小 | `new Decimal(a).gte(b)` | `a >= b` |
| 加法运算 | `a.add(b)` | `a + b` 或 `a += b` |
| 转换为 number | `a.toNumber()` | `Number(a)` |
| 赋值 | `state.number = new Decimal(val)` | `state.number = val` |

**常见错误**：
```ts
// ❌ Decimal 不能赋值给 number
state.dimensionCrystals += 1;

// ✅ 正确
state.dimensionCrystals = state.dimensionCrystals.add(1);
```

---

### 1.3 Pinia store 的 ref 自动解包

在 Pinia 的 `defineStore` 中，返回值里的 `ref` 会被**自动解包**：

```ts
// store 内部
const stateVersion = ref<number>(0);  // 类型是 Ref<number>

return {
  stateVersion,  // 返回时会被解包
};
```

```ts
// 在组件中使用
gameStore.stateVersion;   // ✅ 正确：已经是 number
gameStore.stateVersion++;  // ✅ 正确：直接操作
gameStore.stateVersion.value;  // ❌ 错误：Ref 已被解包，没有 .value
```

---

## 二、Vue 3 `<script setup>` 规范

### 2.1 事件发射：不能用 `$emit()`

在 Options API 或 `<script>`（非 setup）中可以用 `this.$emit()`，但在 `<script setup>` 中**必须**先声明再调用：

```vue
<script setup lang="ts">
// ✅ 正确：先声明
const emit = defineEmits<{
  (e: 'close'): void;
  (e: 'switch', dimId: DimensionId): void;
}>();

// 在模板中使用
</script>

<template>
  <button @click="emit('close')">关闭</button>
</template>
```

```vue
<!-- ❌ 错误：模板里 $emit 是 undefined -->
<button @click="$emit('close')">关闭</button>
```

**影响范围**：本项目共 8 个组件需要修复此问题。

---

### 2.2 `defineProps` 的解构用法

```vue
<script setup lang="ts">
// ✅ 正确：用解构方式
const { visible } = defineProps<{ visible: boolean }>();

// 在模板中直接用 visible
</script>

<template>
  <div v-if="visible">内容</div>
</template>
```

---

### 2.3 强制子组件重新渲染的正确方式

**错误做法**：给子组件加 `:key="stateVersion"`
```vue
<!-- ❌ 错误：stateVersion 每 tick 都变，组件会不断销毁重建 -->
<ChallengePanel :key="gameStore.stateVersion" />
```

**正确做法**：在子组件内部用 `watch` + 局部 `dataVersion`
```ts
// ChallengePanel.vue 内部
const dataVersion = ref(0);
watch(() => gameStore.stateVersion, () => {
  dataVersion.value++;
});

// 模板中
<div v-for="item in list" :key="item.id + '-' + dataVersion" />
```

---

## 三、Capacitor Android 打包经验

### 3.1 构建方式选择

| 方式 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 本地 Android Studio | 调试方便 | 环境配置复杂 | 开发阶段 |
| 本地命令行 | 简单 | 需要完整 Android SDK | 熟悉 Android 开发的用户 |
| **GitHub Actions** | 无需本地环境，自动化 | 需要等待 5-10 分钟 | **推荐：持续构建** |
| CodeMagic | 专业 CI/CD | 配置复杂 | 商业项目 |

**结论**：对于个人开发者，GitHub Actions 是最简单可靠的方案。

---

### 3.2 APK 签名问题（重要！）

**问题**：Android 不允许覆盖安装签名不同的 APK。如果每次构建都生成新的 `debug.keystore`，用户必须卸载旧版才能安装新版。

**解决方案**：在 GitHub Actions 中用固定参数生成 `debug.keystore`：

```yaml
- name: Generate fixed debug keystore
  run: |
    keytool -genkeypair \
      -dname "CN=InfiniteNumeral, OU=Dev, O=InfiniteNumeral, C=CN" \
      -alias androiddebugkey \
      -keypass android \
      -keystore android/app/debug.keystore \
      -storepass android \
      -keyalg RSA \
      -keysize 2048 \
      -validity 10000
```

同时在 `build.gradle` 中配置签名：
```gradle
android {
    signingConfigs {
        debug {
            storeFile file("debug.keystore")
            storePassword "android"
            keyAlias "androiddebugkey"
            keyPassword "android"
        }
    }
    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
    }
}
```

**注意**：修改签名后，用户需要**卸载旧版本**一次。之后所有新版本都可以直接覆盖安装。

---

### 3.3 versionCode 必须递增

Android 只允许安装 `versionCode` 更大或相等的 APK：

```gradle
android {
    defaultConfig {
        versionCode 2  // 每次发布必须 +1
        versionName "2.0"
    }
}
```

**建议**：每次推送构建前，手动递增 `versionCode`。

---

## 四、常见 Bug 及修复方案

### Bug 1：按钮点击无反应

**原因**：`<script setup>` 中模板用了 `$emit()` 而不是 `emit()`。

**排查方法**：
```bash
# 全局搜索是否有遗留的 $emit
grep -rn '\$emit' src/
```

**修复**：全部改为 `emit()`。

---

### Bug 2：UI 不更新，但数据已变化

**原因**：`computed` 直接访问 `markRaw` 的对象，Vue 无法追踪变化。

**修复**：在 `computed` 中加入 `void gameStore.stateVersion`：
```ts
const myComputed = computed(() => {
  void gameStore.stateVersion;  // 强制追踪
  return gameStore.gameState.someField;
});
```

---

### Bug 3：快速点击按钮时文字被选中

**原因**：浏览器默认允许用户选择文本，快速双击会触发文本选择。

**修复**：在全局 CSS 中为所有按钮添加：
```css
button {
  user-select: none;
  -webkit-user-select: none;
  -webkit-tap-highlight-color: transparent;
  touch-action: manipulation;
}
```

---

### Bug 4：组件闪退（打开后立即关闭）

**原因**：给组件添加了 `:key="gameStore.stateVersion"`，而 `stateVersion` 每个 tick 都变化，导致组件不断销毁重建。

**修复**：移除 `:key`，改用组件内部的 `watch` + `dataVersion` 方案（见 2.3）。

---

## 五、Git 工作流

### 5.1 提交规范

建议使用常规提交格式：
```
feat: 添加新手指引系统
fix: 修复挑战面板闪退问题
fix: 修复 stateVersion 类型错误
chore: 更新依赖版本
```

### 5.2 GitHub Actions 自动构建

每次推送到 `master` 分支，GitHub Actions 自动构建新的 APK 并作为 Artifact 上传，保留 30 天。

**下载地址**：
```
https://github.com/netbaker/infinite-numeral/actions
→ 点击最新构建
→ 下载 "infinite-numeral-debug-apk"
```

---

## 六、发布建议

### 6.1 发布就绪度评估（v2.0）

| 维度 | 评分 | 说明 |
|------|------|------|
| Bug 修复 | 9/10 | 主要 Bug 已修复，可能有边缘情况 |
| 功能完整性 | 9/10 | 核心系统 + 辅助系统全部完成 |
| 用户体验 | 7/10 | 有新手指引，但平衡性可能需要调整 |
| 性能 | 8/10 | `markRaw` 优化后性能良好 |
| 存档兼容性 | 6/10 | 版本升级时可能需要重置存档 |

**总体评分：8/10**

**结论**：可以发布，但建议以 **Beta 形式**推出，收集用户反馈后迭代。

---

### 6.2 发布阶段建议

1. **内测阶段**（当前）：邀请 5-10 人测试，收集反馈
2. **公测阶段**：发布 Beta 版本，广泛收集反馈
3. **正式发布**：修复主要问题后，发布 v1.0 正式版

---

## 七、工具和资源

### 7.1 有用的命令

```bash
# 查看 Git 提交历史
git log --oneline

# 查看 TypeScript 错误
npm run type-check

# 本地构建 Web 应用
npm run build

# 同步 Capacitor
npx cap sync android
```

### 7.2 调试技巧

- **Chrome DevTools**：连接手机后，在 Chrome 中访问 `chrome://inspect`，可以调试 Android WebView
- **Vue DevTools**：如果构建时包含 DevTools，可以在手机上查看 Vue 组件状态
- **console.log**：在关键位置添加日志，通过 Chrome DevTools 查看

---

## 八、后续优化方向

1. **性能优化**：
   - 使用 `shallowRef` 替代 `reactive`
   - 虚拟滚动长列表（成就、挑战）
   - Web Worker 处理复杂计算

2. **用户体验**：
   - 添加更多动画效果
   - 优化移动端布局
   - 添加音效和背景音乐

3. **功能扩展**：
   - 云存档（Google Play Games / iCloud）
   - 排行榜
   - 更多维度和挑战

4. **技术债务**：
   - 补充单元测试
   - 迁移到 Vite 新版本
   - 升级 Capacitor 到最新版

---

*最后更新：2026-06-07*
